<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.TextArea?>
<?import javafx.scene.layout.GridPane?>
<?import javafx.scene.layout.RowConstraints?>
<?import javafx.scene.layout.ColumnConstraints?>
<?import main.model.EnumChoiceBox?>
<GridPane fx:controller="main.MainController"
          xmlns:fx="http://javafx.com/fxml"
          prefHeight="-Infinity"
          prefWidth="-Infinity"
          style="-fx-padding: 16 16 16 16; -fx-background-color: #F6F6F6;">

    <VBox GridPane.rowIndex="0"
          style="-fx-padding: 16 16 16 16;">

        <Label text="Input"
               style="-fx-padding: 0 0 16 0; -fx-font-weight: bold; -fx-wrap-text:true; -fx-font-size: 26;"/>

        <HBox spacing="10"
              alignment="CENTER_LEFT"
              style="-fx-padding: 0 0 16 0;">

            <Label text="RegEx:"/>

            <VBox spacing="10"
                  alignment="CENTER">

                <TextField fx:id="regexField"
                           text=""/>
            </VBox>

            <EnumChoiceBox fx:id="operatorCheckbox"
                           enumType="main.model.Operator"
                           prefHeight="21.0"
                           maxWidth="92.0"/>
        </HBox>
    </VBox>

    <HBox spacing="10"
          GridPane.rowIndex="1"
          style="-fx-padding: 16 16 16 16;">

        <Button fx:id="runButton"
                text="Run"
                onAction="#onResultButton"/>

        <Button fx:id="addRegex"
                text="Add"
                onAction="#addRegex"/>

        <Button fx:id="resetRegex"
                text="Reset"
                onAction="#resetRegex"/>

        <Button fx:id="runExample"
                text="Example"
                onAction="#onExampleButton"/>

    </HBox>

    <VBox GridPane.rowIndex="2"
          style="-fx-padding: 16 16 16 16;">

        <Label text="Log"
               style="-fx-padding: 0 0 16 0; -fx-font-weight: bold; -fx-wrap-text:true; -fx-font-size: 26;"/>

        <ListView fx:id="logList"/>
    </VBox>

    <columnConstraints>
        <ColumnConstraints percentWidth="100"/>
    </columnConstraints>
    <rowConstraints>
        <RowConstraints percentHeight="60" minHeight="10.0" prefHeight="106.0" vgrow="ALWAYS"/>
        <RowConstraints percentHeight="10" minHeight="10.0" prefHeight="106.0" vgrow="ALWAYS"/>
        <RowConstraints percentHeight="30" minHeight="10.0" prefHeight="106.0" vgrow="ALWAYS"/>
    </rowConstraints>

</GridPane>